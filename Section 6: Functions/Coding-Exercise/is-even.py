# Functions #6: is even
# Define a function called is_even that takes in one argument, and returns True if the passed in value ir even and False if not.

def is_even(a):
    if a % 2 == 0:
        return True
    else:
        return False